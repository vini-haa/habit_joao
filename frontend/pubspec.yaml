name: frontend
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.1
  flutter_heatmap_calendar: ^1.0.5
  file_picker: ^8.0.5 # Para selecionar/salvar arquivos
  path_provider: ^2.1.3 # Para obter caminhos de diretório
  share_plus: ^9.0.0 # Opcional, para compartilhar o arquivo exportado
  visibility_detector: ^0.4.0+2 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true
