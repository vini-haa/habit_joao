name: frontend
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.1
  flutter_heatmap_calendar: ^1.0.5
  file_picker: ^10.1.9 # Para selecionar/salvar arquivos
  path_provider: ^2.1.3 # Para obter caminhos de diretório
  share_plus: ^11.0.0 # Opcional, para compartilhar o arquivo exportado
  intl: ^0.19.0 # Ou a versão mais recente que você adicionou
  flutter_local_notifications: ^17.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true
